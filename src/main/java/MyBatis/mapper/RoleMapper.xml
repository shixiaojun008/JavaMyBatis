<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="MyBatis.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="MyBatis.entity.Role">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Jun 09 13:14:28 CST 2021.
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
        <result column="note" property="note" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="roleCols">
        id
        , role_name as roleName, note
    </sql>

    <insert id="insertRole" parameterType="role" useGeneratedKeys="true" keyProperty="id">
        insert into t_role(role_name, note)
        values (#{roleName}, #{note})
    </insert>

    <delete id="deleteRole" parameterType="long">
        delete
        from t_role
        where id = #{id}
    </delete>

    <update id="updateRole" parameterType="role">
        update t_role
        set role_name = #{roleName},
            note      = #{note}
        where id = #{id}
    </update>

    <select id="findRoles" parameterType="string" resultType="role">
        select
        <include refid="roleCols"></include>
        from t_role
        where role_name like concat('%', #{roleName}, '%')
    </select>


    <select id="getRole" parameterType="long" resultType="role">
        select
        <include refid="roleCols"></include>
        from t_role
        where id = #{id}
    </select>

    <select id="findRolesByParams" resultType="role">
        select
        <include refid="roleCols"></include>
        from t_role
        where role_name like concat('%', #{params.roleName}, '%')
        and note like concat('%', #{params.note}, '%')
        limit #{page.start}, #{page.limit}
    </select>

    <select id="countRole" parameterType="MyBatis.entity.CountRoleParams" statementType="CALLABLE">
        {CALL sp_count_role(
            #{roleName, mode=IN, jdbcType=VARCHAR},
            #{total, mode=OUT, jdbcType=INTEGER},
            #{execDate, mode=OUT, jdbcType=DATE}
            )}
    </select>

</mapper>